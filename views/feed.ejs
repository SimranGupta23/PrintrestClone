<% include ./partials/header.ejs %>
    <!-- <div class="w-full min-h-screen bg-zinc-800 px-10 py-8 text-white">
        <div class="cards flex flex-wrap gap-10">
            <% posts.forEach(function(elem){ %>
                <div class="card w-32">
                    <div class="w-full h-40 rounded-lg overflow-hidden">
                        <img class="w-full h-full object-cover" src="/images/uploads/<%= elem.image %>" alt="">
                    </div>
                    <h5 class="mt-2 text-sm">
                        <%= elem.title %>
                    </h5>
                    <h6 class="text-xs font-semibold opacity-50">@<%= elem.user.username %>
                    </h6>
                </div>
                <% }) %>
        </div>
    </div> -->
    <h1 class="text-center text-white mb-5 mt-3 text-3xl font-bold ">ALL &nbsp; POSTS</h1>
    <div class="container">
        <% posts.forEach(function(elem){ %>
            <div class="box">
                <!-- <a href="ttps://oriontralelblog.com/wp-content/uploads/2019/08/IMG_6310.jpg" > -->
                <img src="/images/uploads/<%= elem.image %>" alt="image"></a>
                <div class="caption">
                    <%= elem.title %>
                </div>
                <h6 class="text-xs font-semibold text-white">@<%= elem.user.username %></h6>
            </div>
        <% }) %>
    </div>
    <% include ./partials/footer.ejs %>


        <script src="https://code.jquery.com/jquery-3.3.1.js"></script>
        <script src="app.js"></script>